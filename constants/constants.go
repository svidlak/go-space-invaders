package constants

const (
	PlayerSpeed          = 300
	AlienMovementTimeout = 0.5
	AlienMovementOffset  = 5
)

type Direction string

const (
	Right Direction = "right"
	Left  Direction = "left"
	Down  Direction = "down"
)
